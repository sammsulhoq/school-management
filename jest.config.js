module.exports = {
  testEnvironment: 'node',        // Ensures compatibility with Node.js
  testTimeout: 30000,            // Adjust timeout for longer-running tests
  verbose: true,                 // Enables detailed test output
};
